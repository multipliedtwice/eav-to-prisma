# EAV Database (where model definitions are stored)
DATABASE_URL="file:./prisma/eav.db"

# Content Database (for generated schema)
# This is what goes in the generated schema's datasource
CONTENT_DATABASE_URL="file:./prisma/content.db"
